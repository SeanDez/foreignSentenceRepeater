

export default class Utilities {

   /* one callback generates an input
         characteristics: either checks for a file, returning boolean. Or 
   */
   // public getDataAndLoopUntilValid <C1, C2>(
   //    callback: Callback
   //    , callbackArgs: Args
   //    , printInvalidMessage: () => void) {
   //    let continueLooping = true;
   //    let userInput;

   //    // force loop to continue until callback validates with true
   //    while (continueLooping) {
   //       const isValid: boolean = callback(callbackArgs);
         
   //       if (isValid) {
   //          continueLooping = false;
   //          userInput = callbackArgs.rawUserInput;
   //       }
   //    }

   //    // user input is validated
   //    const validatedInput = userInput;

   //    return validatedInput;
   // }

   
}